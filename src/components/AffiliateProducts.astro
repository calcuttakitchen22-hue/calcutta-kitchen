---
import { products } from "@/data/products";

const productsById = Object.fromEntries(
  products.map(product => [product.id, product])
);
---

<pre>
{JSON.stringify(productsById["atomberg-mixer-grinder"], null, 2)}
</pre>